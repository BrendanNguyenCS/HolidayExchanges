@model HolidayExchanges.ViewModels.LoginViewModel

@{
    ViewBag.Title = "Login";
}

<style>
    .btn {
        border-radius: 8px;
    }
</style>

<div class="container">

    <section id="LoginTitle">
        <div class="row">
            <div class="page-header col-12">
                <h1 class="text-center display-4">Login</h1>
                <hr />
            </div>
        </div>
    </section>

    <section id="LoginForm">
        <div class="container body-content">
            @using (Html.BeginForm("Login", "Login", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="container form-horizontal">
                    <div class="row">
                        <div class="col-12">
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

                            <div class="form-group row">
                                @Html.LabelFor(model => model.UserName, htmlAttributes: new { @class = "col-form-label col-sm-3 offset-md-1 col-md-2 offset-lg-2 col-lg-2" })
                                <div class="col-12 col-md-7 col-lg-5">
                                    @Html.EditorFor(model => model.UserName, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.UserName, "", htmlAttributes: new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-group row">
                                @Html.LabelFor(model => model.Password, htmlAttributes: new { @class = "col-form-label col-sm-3 offset-md-1 col-md-2 offset-lg-2 col-lg-2" })
                                <div class="col-12 col-md-7 col-lg-5">
                                    @Html.EditorFor(model => model.Password, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.Password, "", htmlAttributes: new { @class = "text-danger" })
                                </div>
                            </div>

                            <div class="form-group row">
                                <div class="offset-sm-3 col-md-9 offset-md-3 col-md-9 offset-lg-4 col-lg-8">
                                    @Html.Button("Login", "btn btn-primary", "LoginSubmit", HtmlExtensionsButton.HtmlButtonTypes.submit)
                                    <a class="btn btn-secondary" href="@Url.Action("Register", "Login")" id="RegisterButton">Register</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </section>
</div>

<!-- #region AJAX addition -->
@*
    <script type="text/javascript">
        $(document).ready(function () {
            $('#LoginSubmit').click(function () {
                var token = $('[name=__RequestVerificationToken]').val();
                $.ajax({
                    url: '@Url.Action("ValidateLogin", "Login")',
                    async = false,
                    method: 'POST',
                    cache: false,
                    headers: { '__RequestVerificationToken': token },
                    data: {model: JSON.stringify(@Model) },
                    contentType: 'application/json; charset=utf-8',
                    dataType: 'json',
                    success: function (response) {
                        if (response.success) {
                            if (response.redirect) {
                                window.location.href = response.redirectUrl;
                            }
                            else {
                                window.location.href = '@Url.Action("Index", "Home")';
                            }
                        }
                        else {
                            alert(response.ex);
                        }
                    }
                });
            });
        });
    </script>
*@
<!-- #endregion -->